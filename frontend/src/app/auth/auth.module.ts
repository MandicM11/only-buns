import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';  // Ako koristite bilo koju funkcionalnost iz CommonModule

@NgModule({
  imports: [CommonModule],
  // Nema declarations ili exports za standalone komponente
})
export class AuthModule {}
